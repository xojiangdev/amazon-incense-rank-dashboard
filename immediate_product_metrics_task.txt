立即只执行 Amazon 当前在售 Listing 的 Review Rating 与评价数同步，不执行自然排名采集。运行 cd /home/ubuntu/amazon-incense-rank-dashboard && npx tsx scripts_export_product_metric_targets.ts，读取 /home/ubuntu/amazon-incense-rank-dashboard/private_spapi_import/product_metric_targets.json 中全部 42 个 marketplace+ASIN 目标（当前应为 US 36、CA 6、JP 0）。对每个目标使用已连接的 Sorftime MCP product_detail，amz_site 必须按 marketplace 精确传入 US 或 CA。只接受 data.asin 与目标 ASIN 完全一致的返回；rating 取 data.star_rating，reviewCount 取 data.review_count，source 固定为 sorftime_product_detail。准确返回 ASIN 但没有评分字段时，rating 和 reviewCount 均记 null；任何调用失败、ASIN 不匹配、重复、遗漏或字段越界时停止整批，不得部分写入。全部42个完成后，以当前 ISO 时间作为 observedAt，保存 /home/ubuntu/amazon-incense-rank-dashboard/private_spapi_import/product_metrics_latest.json，格式 {observedAt,metrics:[{marketplace,asin,rating,reviewCount,source}]}，然后运行 cd /home/ubuntu/amazon-incense-rank-dashboard && npx tsx scripts_import_product_metrics.ts。必须核对 updated=42，并报告有评分数、无评分数、平均评分和评价总数。禁止模拟、猜测或复用其他站点数据。评分和评价数是公开商品详情数据，不是 SP-API 指标。
